[package]
name = "auction-gateway"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Network edge: WebSocket bid transport, auth, rate limiting, risk pre-checks"

# Phase 4 crate, currently a doc comment. The dependency block it will need — axum, tower-http,
# tokio, dashmap and the metrics exporter — is deliberately absent until there is code to use it:
# every `cargo test --workspace` pays to compile whatever is listed here, and making the whole
# workspace's test cycle slower to reserve names for later is a bad trade.
[dependencies]
auction-proto.workspace = true
auction-core.workspace = true
auction-engine.workspace = true
