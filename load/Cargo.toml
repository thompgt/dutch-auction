[package]
name = "auction-load"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Thundering-herd load generator and latency analysis"
publish = false

# Phase 7 binary, currently a stub main. tokio, hdrhistogram and clap land with the generator
# that needs them.
[dependencies]
auction-proto.workspace = true
